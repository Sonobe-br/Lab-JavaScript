/* Escreva uma função que retorne o maior número */

let maxValue = max (1500, 8500);
console.log (maxValue);

function max  (number1, number2) {
    
    return number1 > number2 ? number1 : number2; /* usando operador ternário */
    
}